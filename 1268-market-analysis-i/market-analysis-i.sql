# Write your MySQL query statement below
select u.user_id AS buyer_id,join_date,sum(case when year(o.order_date) =2019 then 1 else 0 END) as orders_in_2019 from users u left join orders o on u.user_id = o.buyer_id GROUP BY U.USER_ID;