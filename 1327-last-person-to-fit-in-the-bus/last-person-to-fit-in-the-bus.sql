# Write your MySQL query statement below
with total_table as(select person_id, person_name, weight, turn, sum(weight) over(order by turn) as total_weight from queue) select person_name from total_table where total_weight <= 1000 order by  total_weight desc limit 1;